<div class="flyntComponent" is="flynt-hero-image-text">
  {% if posterImage %}
  <img class="figure-image figure-image--desktop lazyload"
    src="{{ posterImage.src|resize(32) }}"
    data-srcset="{{function('wp_get_attachment_image_srcset', posterImage.id)}}"
    data-sizes="auto">
  {% endif %}
  {% if posterImageMobile %}
  <img class="figure-image figure-image--mobile lazyload"
    src="{{ posterImageMobile.src|resize(32) }}"
    data-srcset="{{function('wp_get_attachment_image_srcset', posterImageMobile.id)}}"
    data-sizes="auto">
  {% endif %}
  {% if contentHtml %}
    <div class="content grid8">
      <div class="content-inner contentAlign--{{ contentAlignment }}">
        <div>{{ contentHtml }}</div>
        {% if buttonLink %}
          <a href="{{ buttonLink.url }}"{% if buttonLink.target %} target="{{ buttonLink.target }}" rel="noreferrer noopener"{% endif %} class="button button--link">
            {{ buttonLink.title|e }}
          </a>
        {% endif %}
      </div>
    </div>
  {% endif %}
</div>
